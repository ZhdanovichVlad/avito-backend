FROM golang:1.23 as builder

WORKDIR /app

COPY go.mod go.sum ./

RUN go mod dowload

COPY ./backend .

RUN CGO_ENABLE=0 go build -o bin/api cmd/api/main.go

FROM scratch

COPY --from=builder /app/bin/api .

EXPOSE ${API_PORT}

ENTRYPOINT ["./api"]


